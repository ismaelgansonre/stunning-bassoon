# Editor.js Interface for Directus

This interface embeds Editor.js inside Directus.

## Build

From this directory:

```
pnpm install
pnpm run build
```

This produces a build in the extension output directory for Directus to load.
